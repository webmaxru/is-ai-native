{
  "name": "is-ai-native-backend",
  "version": "1.0.0",
  "description": "Backend API for is-ai-native scanner",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --runInBand",
    "test:unit": "node --experimental-vm-modules node_modules/.bin/jest tests/unit --runInBand",
    "test:contract": "node --experimental-vm-modules node_modules/.bin/jest tests/contract --runInBand",
    "test:integration": "node --experimental-vm-modules node_modules/.bin/jest tests/integration --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "transform": {}
  }
}
